"""Built-in training invariants (registered).

The invariant DSL framework (``InvariantError`` / ``evaluate_check`` in
``lighttrain.invariants._dsl``) stays in core; the concrete ``@register`` checks
live here and are picked up by auto-discovery.
"""
